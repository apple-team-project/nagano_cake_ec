<h2>商品一覧(全<%=@items.count %>件)</h2>
<% @items.each do |item| %>
  <div>
  <%=image_tag item.get_item_image(100,100) %><br>
  <%=item.name %><br>
  ¥<%=item.price %>
  </div>
  <% end %>
  <%= paginate @items %>